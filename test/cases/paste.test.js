describe('Paste test', function () {
  describe('textOnly', function () {
    it('shoud be a function', function () {
      (typeof feu.paste.textOnly === 'function').should.equal(true)
    })
  })
})
